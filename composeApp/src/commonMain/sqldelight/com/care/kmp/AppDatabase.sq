CREATE TABLE user (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL
);

insertUser:
INSERT INTO user(name)
VALUES (?);

selectAll:
SELECT * FROM user;

updateUser:
UPDATE user
SET name = ?
WHERE id = ?;

deleteUser:
DELETE FROM user
WHERE id = ?;